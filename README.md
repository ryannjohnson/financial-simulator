# Financial Planner

This is a self-contained webapp that lets you see your net worth over time.

## Quickstart

To run the example:

```bash
$ npm install
$ cd example
$ npm install
$ npm run start
```
